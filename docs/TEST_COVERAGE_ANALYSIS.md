# Анализ покрытия тестами проблемы резолва актуаторов

## Сопоставление проблем и тестов

### ✅ Проблема 1: Каналы не запрашиваются при начальной загрузке

**Тест:** `test-hypothesis-1-channels-not-initial-load.js`

**Покрытие:** ✅ **ПОЛНОЕ**
- ✅ Проверяет массовый GET при старте
- ✅ Проверяет отсутствие каналов в запросе
- ✅ Проверяет отсутствие состояний каналов после загрузки
- ✅ Подсчитывает процент отсутствующих каналов

**Результат прогона:** Частично опровергнута (каналы запрашиваются, но не все)

---

### ✅ Проблема 2: Частичные обновления каналов без поля `bind`

**Тест:** `test-hypothesis-2-partial-updates-without-bind.js`

**Покрытие:** ✅ **ПОЛНОЕ**
- ✅ Проверяет процент обновлений с полем `bind`
- ✅ Проверяет частоту частичных обновлений без `bind`
- ✅ Проверяет корреляцию между `_context` и отсутствием `bind`
- ✅ Собирает статистику по каналам

**Результат прогона:** Подтверждена (100% обновлений без bind в первом прогоне)

---

### ⚠️ Проблема 3: Ограничительное условие дозапроса `bind`

**Тест:** `test-hypothesis-3-selected-actuator-restriction.js`

**Покрытие:** ⚠️ **ЧАСТИЧНОЕ**
- ✅ Проверяет дозапросы для выбранного актуатора
- ✅ Проверяет дозапросы для невыбранных актуаторов
- ❌ **ПРОБЛЕМА:** Перехватывает ВСЕ запросы каналов, включая начальную загрузку
- ❌ Не различает дозапросы от начальной загрузки
- ❌ Не проверяет реальную логику из monitor.js (строка 3330)

**Требуется доработка:** Фильтровать только дозапросы, а не все запросы каналов

---

### ⚠️ Проблема 4: Асинхронность загрузки связанных устройств

**Тест:** `test-hypothesis-4-async-linked-devices.js`

**Покрытие:** ⚠️ **ЧАСТИЧНОЕ**
- ✅ Проверяет количество каналов с `bind`, но без `linkedDevice`
- ✅ Проверяет время между появлением `bind` и загрузкой устройства
- ⚠️ Не проверяет эффективность механизма `requestMissingDevice`
- ❌ Не проверяет реальную логику из monitor.js (строки 1881-1883, 1927-1929, 1956-1958)
- ❌ Не проверяет, что `requestMissingDevice` вызывается при наличии `bind`

**Требуется доработка:** Добавить проверку вызова `requestMissingDevice` и его эффективности

---

### ⚠️ Проблема 5: Неэффективный поиск связанных устройств

**Тест:** `test-hypothesis-5-inefficient-device-search.js`

**Покрытие:** ⚠️ **ЧАСТИЧНОЕ**
- ✅ Проверяет случаи, когда `bind` не совпадает с `id`
- ✅ Проверяет эффективность fallback поиска по `code` и `name`
- ✅ Анализирует форматы `bind` и `id`
- ❌ Не проверяет реальную логику из monitor.js (строки 1871-1878, 1917-1924, 1946-1953)
- ❌ Не проверяет все возможные варианты форматов

**Требуется доработка:** Добавить проверку реальной логики поиска из monitor.js

---

## Пробелы в покрытии

### ❌ Цепочка проблем (не покрыта)

**Проблема:** Тесты проверяют отдельные проблемы, но не проверяют всю цепочку:
1. Старт приложения → Каналы не загружаются
2. Частичные обновления → Каналы приходят без `bind`
3. Ограничение дозапроса → Дозапрос работает только для выбранного актуатора
4. Отсутствие связанных устройств → Устройства из `bind` не загружены
5. Результат → UI показывает "(не привязан)"

**Требуется:** Интеграционный тест, проверяющий всю цепочку от начала до конца

---

### ❌ Результат в UI (не покрыт)

**Проблема:** Тесты не проверяют, что UI действительно показывает "(не привязан)" вместо реальных привязок.

**Требуется:** Тест, который:
- Симулирует UI отображение
- Проверяет, что каналы с `bind` показываются как "(не привязан)"
- Проверяет корректность отображения после резолва

---

### ❌ Механизм повторного резолва (не покрыт)

**Проблема:** В документе указано требование "Добавить механизм повторного резолва", но нет теста, который проверяет:
- Пересчет `linkedDevice` при получении новых данных
- Обновление UI при изменении состояния резолва

**Требуется:** Тест для проверки повторного резолва

---

### ❌ Интеграция всех проблем вместе (не покрыта)

**Проблема:** Тесты проверяют проблемы изолированно, но не проверяют, как они взаимодействуют друг с другом.

**Требуется:** Комплексный интеграционный тест, который:
- Симулирует реальный сценарий использования
- Проверяет все проблемы одновременно
- Проверяет результат в UI

---

## Рекомендации по улучшению покрытия

### 1. Доработать существующие тесты

- **Тест 3:** Фильтровать только дозапросы, проверять реальную логику из monitor.js
- **Тест 4:** Добавить проверку вызова `requestMissingDevice` и его эффективности
- **Тест 5:** Добавить проверку реальной логики поиска из monitor.js

### 2. Добавить новые тесты

- **Тест 6:** Интеграционный тест цепочки проблем
- **Тест 7:** Тест результата в UI
- **Тест 8:** Тест механизма повторного резолва
- **Тест 9:** Комплексный интеграционный тест всех проблем

### 3. Улучшить существующие тесты

- Добавить проверку реальной логики из monitor.js (не только симуляцию)
- Добавить проверку временных характеристик (задержки, таймауты)
- Добавить проверку граничных случаев

---

## Итоговая оценка покрытия

| Проблема | Покрытие | Статус |
|----------|----------|--------|
| 1. Каналы не запрашиваются при начальной загрузке | ✅ Полное | Готово |
| 2. Частичные обновления без `bind` | ✅ Полное | Готово |
| 3. Ограничительное условие дозапроса | ⚠️ Частичное | Требует доработки |
| 4. Асинхронность загрузки связанных устройств | ⚠️ Частичное | Требует доработки |
| 5. Неэффективный поиск связанных устройств | ⚠️ Частичное | Требует доработки |
| Цепочка проблем | ❌ Не покрыта | Требуется новый тест |
| Результат в UI | ❌ Не покрыт | Требуется новый тест |
| Механизм повторного резолва | ❌ Не покрыт | Требуется новый тест |

**Общая оценка покрытия: 60%**

- ✅ Основные проблемы покрыты (5 из 5)
- ⚠️ 3 теста требуют доработки
- ❌ 3 важных аспекта не покрыты

---

## Выводы

1. **Тесты покрывают основные проблемы**, но требуют доработки для полного покрытия
2. **Отсутствуют интеграционные тесты** для проверки цепочки проблем и результата в UI
3. **Тесты частично симулируют поведение**, но не всегда проверяют реальную логику из monitor.js
4. **Рекомендуется** доработать существующие тесты и добавить новые для полного покрытия






